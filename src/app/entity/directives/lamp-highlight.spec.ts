import { LampHighlight } from './lamp-highlight';

describe('DeviceHighlight', () => {
  it('should create an instance', () => {
    const directive = new LampHighlight();
    expect(directive).toBeTruthy();
  });
});
