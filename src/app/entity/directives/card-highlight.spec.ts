import { CardHighlight } from './card-highlight';

describe('CardHighlight', () => {
  it('should create an instance', () => {
    const directive = new CardHighlight();
    expect(directive).toBeTruthy();
  });
});
